# 智能问数技能
适用场景：用户想"查某个数 / 问某个指标 / 对比某段时间"时使用本技能。
标准打法：
1. 先用 TableRAG 选出相关表和字段（不要贪多，只保留和问题有关的）。
2. 用 NL2SQL 三段式：改写问题 → 说明思路 → 生成 SQL（temperature=0 保证稳定）。
3. 在真实数据库执行 SQL，把结果返回给用户。
4. 如果 SQL 报错，把错误信息回喂模型重试一次。
注意：只能使用 TableRAG 筛选出来的表和字段，禁止臆造不存在的表或列。
